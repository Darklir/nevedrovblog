<!DOCTYPE html>
<html lang="en">
  <head>

	<title>Blog by Nevedrov</title>

  	<%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  	<%= javascript_include_tag "application" %>
  	<%= csrf_meta_tags %>

  </head>

  <body role="document" class="<%= controller_name %> <%= action_name %>">

    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "BLOG", articles_path, class: "navbar-brand" %>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-left">
            <li <%= yield :home %> ><%= link_to "Home", articles_path %></li>
            <li <%= yield :about %> ><%= link_to "CV",'/about' %></li>
            <li <%= yield :contact %> ><%= link_to "Contact",'/contact' %></li>
         </ul>
         <% if current_user %>
          <ul class="nav navbar-nav navbar-right">
         	<li><%= link_to current_user.first_name, user_path(current_user) %></li>
         	<li><%= link_to "Log Out", logout_path, method: :delete %></li>
          </ul>
         <% else %>
          <ul class="nav navbar-nav navbar-right">
            <li <%= yield :signin %>><%= link_to "Sign In", "/login" %></li>
            <li <%= yield :signup %>><%= link_to "Sign Up", new_user_path %></li>
          </ul>
          <% end %>
        </div>
      </div>
    </nav>

  	
  	<div class="container">
       
      	<%= yield %>
    
	  	<footer class="footer">
	        <p>&copy;Nevedrov 2015</p>
	    </footer>

	</div>

	
  </body>
</html>
